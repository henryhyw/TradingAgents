services:
  tradingagents:
    build: .
    env_file:
      - .env
    environment:
      - TRADINGAGENTS_HOME=/home/appuser/.tradingagents
    volumes:
      - tradingagents_data:/home/appuser/.tradingagents
    tty: true
    stdin_open: true

volumes:
  tradingagents_data:
